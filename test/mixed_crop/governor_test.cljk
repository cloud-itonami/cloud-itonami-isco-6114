(ns mixed-crop.governor-test
  (:require [kotoba.test :refer [deftest is testing]]
            [mixed-crop.store :as store]
            [mixed-crop.governor :as governor]))

(defn- fresh-store []
  (let [st (store/mem-store)]
    (store/register-plot! st {:plot-id "plot-1" :name "South Field Mix"})
    st))

(deftest ok-on-clean-tend
  (let [st (fresh-store)
        proposal {:op :tend :effect :propose :confidence 0.9 :stake :low}
        v (governor/check {:plot-id "plot-1"} {} proposal st)]
    (is (:ok? v))
    (is (not (:hard? v)))
    (is (not (:escalate? v)))))

(deftest hard-on-unregistered-plot
  (let [st (fresh-store)
        proposal {:op :tend :effect :propose :confidence 0.9 :stake :low}
        v (governor/check {:plot-id "no-such-plot"} {} proposal st)]
    (is (:hard? v))
    (is (some #(= :no-plot (:rule %)) (:violations v)))))

(deftest hard-on-no-actuation-violation
  (let [st (fresh-store)
        proposal {:op :tend :effect :direct-write :confidence 0.9 :stake :low}
        v (governor/check {:plot-id "plot-1"} {} proposal st)]
    (is (:hard? v))
    (is (some #(= :no-actuation (:rule %)) (:violations v)))))

(deftest escalates-on-operate-near-buyers-visitors
  (let [st (fresh-store)
        proposal {:op :operate-near-buyers-visitors :effect :propose :confidence 0.9 :stake :high}
        v (governor/check {:plot-id "plot-1"} {} proposal st)]
    (is (:escalate? v))
    (is (not (:hard? v)))))

(deftest escalates-on-treatment-application-near-water
  (let [st (fresh-store)
        proposal {:op :treatment-application-near-water :effect :propose :confidence 0.9 :stake :high}
        v (governor/check {:plot-id "plot-1"} {} proposal st)]
    (is (:escalate? v))
    (is (not (:hard? v)))))

(deftest escalates-on-low-confidence
  (let [st (fresh-store)
        proposal {:op :tend :effect :propose :confidence 0.2 :stake :low}
        v (governor/check {:plot-id "plot-1"} {} proposal st)]
    (is (:escalate? v))
    (is (not (:hard? v)))))

(deftest store-records-and-ledger-append-only
  (let [st (fresh-store)]
    (store/commit-record! st {:plot-id "plot-1" :op :harvest})
    (store/append-ledger! st {:disposition :commit})
    (is (= 1 (count (store/records-of st "plot-1"))))
    (is (= 1 (count (store/ledger st))))))
